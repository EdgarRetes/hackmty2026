import type { DashboardOpportunity } from "@/lib/dashboard";
import { Icon } from "../icons";

const tones = { green: "bg-[#dcfce7] text-[#16a34a]", purple: "bg-[#f3eaff] text-[#7c3aed]", blue: "bg-[#e6f3ff] text-[#0875d1]" };

export function OpportunitiesPanel({ opportunities }: { opportunities: DashboardOpportunity[] }) {
  return <section className="min-h-[360px] rounded-xl border border-slate-200 bg-white p-4 shadow-[0_7px_22px_rgba(15,31,68,.025)]"><header className="flex items-center justify-between"><h2 className="flex items-center gap-2 text-lg font-bold text-navy"><Icon name="star" size={19}/>Oportunidades para ti</h2><a href="/invoices" className="text-xs font-medium text-blue-600">Ver todas</a></header><div className="mt-4 space-y-2.5">{opportunities.length ? opportunities.map((opportunity) => <article key={opportunity.id} className="flex items-center gap-3 rounded-xl border border-slate-200 p-3"><div className={`flex h-12 w-12 shrink-0 items-center justify-center rounded-xl ${tones[opportunity.tone]}`}><Icon name={opportunity.icon} size={24}/></div><div className="min-w-0 flex-1"><h3 className="text-sm font-semibold text-navy">{opportunity.title}</h3><p className="mt-1 text-xs leading-4 text-[#52688f]">{opportunity.description}</p><a href={opportunity.href} className="mt-1.5 inline-flex items-center gap-2 text-xs font-semibold text-blue-600">{opportunity.action}<Icon name="arrow" size={14}/></a></div><Icon name="chevronRight" size={17} className="text-[#6b7fa5]"/></article>) : <div className="flex min-h-[250px] items-center justify-center text-center"><div><p className="font-semibold text-navy">Sin oportunidades nuevas</p><p className="mt-1 text-xs text-[#52688f]">Aquí aparecerán acciones respaldadas por tus facturas y ofertas.</p></div></div>}</div></section>;
}
